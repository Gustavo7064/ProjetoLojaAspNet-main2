@{
    ViewData["Title"] = "Meu perfil";
}
<link href="c:\users\julia\desktop\projetolojaaspnet-main2\projetoloja2dsa\wwwroot\css\Perfil.css" rel="stylesheet" />
<div class="perfil-root">
    <div class="container perfil-container">

        <!-- TOPO / CABEÇALHO DO PERFIL -->
        <div class="perfil-header">
            <div class="perfil-user-info">
                <div class="perfil-avatar-ring">
                    <img src="@ViewBag.Foto" alt="Foto do usuário" class="perfil-avatar-img" />
                </div>
                <div>
                    <h1 class="perfil-username">@ViewBag.Nome</h1>
                    <p class="perfil-subtitle">Jogador RocketGames</p>
                    <span class="perfil-status-pill">
                        <span class="perfil-status-dot"></span>
                        Online
                    </span>
                </div>
            </div>

            <div class="perfil-header-actions">
                <form asp-controller="Usuario" asp-action="Logout" method="post">
                    <button type="submit" class="btn-perfil-outline">
                        <i class="fa-solid fa-right-from-bracket me-1"></i> Sair
                    </button>
                </form>
            </div>
        </div>

        <!-- GRID PRINCIPAL: DADOS DA CONTA / SEGURANÇA / EXPERIÊNCIA -->
        <div class="row g-4 mt-3">

            <!-- COLUNA ESQUERDA: DADOS DA CONTA -->
            <div class="col-lg-7">
                <div class="perfil-card">
                    <div class="perfil-card-header">
                        <i class="fa-solid fa-user-gear me-2"></i>
                        <span>Dados da conta</span>
                    </div>
                    <div class="perfil-card-body">
                        <div class="row gy-3">
                            <div class="col-sm-6">
                                <div class="perfil-field">
                                    <span class="perfil-field-label">Nome completo</span>
                                    <span class="perfil-field-value">@ViewBag.Nome</span>
                                </div>
                            </div>
                            <div class="col-sm-6">
                                <div class="perfil-field">
                                    <span class="perfil-field-label">E-mail</span>
                                    <span class="perfil-field-value">@ViewBag.Email</span>
                                </div>
                            </div>
                        </div>

                        <div class="perfil-divider"></div>

                        <div class="perfil-meta">
                            <div>
                                <span class="perfil-meta-label">Tipo de conta</span>
                                <span class="perfil-meta-value">Padrão</span>
                            </div>
                            <div>
                                <span class="perfil-meta-label">Status</span>
                                <span class="perfil-meta-badge">Ativa</span>
                            </div>
                        </div>

                        <div class="mt-3">
                            <a class="btn-perfil-primary"
                               asp-controller="Usuario"
                               asp-action="Editar">
                                <i class="fa-solid fa-pen-to-square me-1"></i>
                                Editar informações
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <!-- COLUNA DIREITA: “ESTATÍSTICAS” / PLACEHOLDER -->
            <div class="col-lg-5">
                <div class="perfil-card perfil-card-compact">
                    <div class="perfil-card-header">
                        <i class="fa-solid fa-chart-line me-2"></i>
                        <span>Resumo da conta</span>
                    </div>
                    <div class="perfil-card-body perfil-stats-grid">
                        <div class="perfil-stat">
                            <span class="perfil-stat-value">0</span>
                            <span class="perfil-stat-label">Jogos comprados</span>
                        </div>
                        <div class="perfil-stat">
                            <span class="perfil-stat-value">0</span>
                            <span class="perfil-stat-label">Favoritos</span>
                        </div>
                        <div class="perfil-stat">
                            <span class="perfil-stat-value">0</span>
                            <span class="perfil-stat-label">Avaliações</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- COLUNA ESQUERDA: SEGURANÇA -->
            <div class="col-lg-6">
                <div class="perfil-card">
                    <div class="perfil-card-header">
                        <i class="fa-solid fa-shield-halved me-2"></i>
                        <span>Segurança</span>
                    </div>
                    <div class="perfil-card-body">
                        <p class="perfil-text">
                            Mantenha sua conta segura. Nunca compartilhe sua senha com outras pessoas
                            e evite usar a mesma senha em outros sites.
                        </p>
                        <a asp-controller="Usuario" asp-action="Login" class="perfil-link">
                            Alterar senha (futuro)
                        </a>
                    </div>
                </div>
            </div>

            <!-- COLUNA DIREITA: EXPERIÊNCIA -->
            <div class="col-lg-6">
                <div class="perfil-card">
                    <div class="perfil-card-header">
                        <i class="fa-solid fa-gamepad me-2"></i>
                        <span>Sua experiência</span>
                    </div>
                    <div class="perfil-card-body">
                        <p class="perfil-text">
                            Em breve você poderá ver aqui seu histórico de compras, lista de desejos,
                            progresso em jogos e muito mais.
                        </p>
                        <span class="perfil-tag">Em desenvolvimento</span>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>
